import { Routes } from "@angular/router";
import { RoomListComponent } from "./room-list.component";

export const routes: Routes = [
    { path: '', component: RoomListComponent }, // Default route
];